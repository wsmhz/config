
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wsmhz/wsmhz-config-repository.git
          username: 787740644@qq.com
          password:
          basedir: F:/idea/wsmhz-config/basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      expose: "*"
  security:
    enabled: false
server:
  port: 8082